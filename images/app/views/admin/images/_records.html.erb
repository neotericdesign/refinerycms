<% if searching? %>
  <h2><%= t('results_for', :scope => 'shared.admin.search', :query => params[:search]) %></h2>
<% end %>
<% if @images.any? %>
  <div class='pagination_container'>
    <%= "<h2>Images tagged \"#{@tag.name}\"</h2>".html_safe if @tag %>
    <%= render :partial => 'images' %>
  </div>
<% else %>
  <p>
    <% unless searching? %>
      <strong>
        <%= t('.no_images_yet') %>
      </strong>
    <% else %>
      <%= t('no_results', :scope => 'shared.admin.search') %>
    <% end %>
  </p>
<% end %>
